var arr = ["ravi","raj",100,200,300];
var array = arr.slice(1,3);  // here we have to store the data in one varible because it will return new array
console.log(array);          // 1 is the position of the element in the array and 3 is the how many element you want +1;

// command to print the element values in the terminal is node sliceArray

var arr1 = [1,2,3,4,5];
var array1 = arr1.slice(1,4);
console.log(array1);  /// we have take some part of the elements in array.there we can use slice method.
