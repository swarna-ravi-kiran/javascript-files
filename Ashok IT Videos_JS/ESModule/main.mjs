import fn, {empName,displayDetails} from "./user.mjs";  //user.mjs file ni link chesi andulo empName,display details ni import chesam

displayDetails();  // we can call directly
console.log(empName);
fn();               //this is defalut import function call

// note: we can import and export lot of named varibales and functions but we can export and import only one defalut function or variable
